import { defineConfig, devices } from '@playwright/test';

/**
 * Playwright config for TheThinker e2e smoke tests (KAN-33).
 * Structure follows the official guide: https://playwright.dev/docs/intro
 *
 * The `webServer` block auto-starts the Vite dev server, so `npm run e2e`
 * works without launching anything first. The landing/login pages render
 * without the backend, keeping the smoke test self-contained.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 120 * 1000,
  },
});
